require "spec_helper"

RSpec.describe TimesBooks do
  it "has a version number" do
    expect(TimesBooks::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
